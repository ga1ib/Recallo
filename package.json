{
  "name": "recallo-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "setup:frontend": "cd frontend && npm install",
    "setup:backend-env": "echo SUPABASE_URL=your_supabase_url > backend/.env && echo SUPABASE_KEY=your_supabase_key >> backend/.env && echo GEMINI_API_KEY=your_gemini_key >> backend/.env",
    "setup:venv": "cd ai-engine && python -m venv .venv && .venv\\Scripts\\activate && pip install -r requirements.txt",
    "setup": "npm run setup:frontend && npm run setup:venv && npm run setup:backend-env"
  }
}
